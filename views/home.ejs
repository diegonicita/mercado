<%- include ("partials/header") %>

    <main>  
        <section> 
            <h2>Basado en tu ultima visita</h2>            
        
        <div class="contenedor-main">        
            <% var toThou = toThousand %>
            <% productos.forEach( p => { %>

            <%- include ("partials/product", {p : p, toThousand: toThou}) %>

            <% }) %>
        </div>

        </section>
        <br>
        <section>
            <h2>Ofertas</h2> 

        <div class="contenedor-main">
            
            <% productos.forEach( p => { %>

                <%- include ("partials/product", {p : p}) %>    
                <% }) %>
        </div>       
        </section>         
    </main>
    <script type="application/javascript">
        function showMsg(item) {
        alert(item);
    }
    </script>
    
    <%- include ("partials/footer") %>